# Local env (do not commit)
.env

# Local runtime data
/data/

# Monitoring data volumes (created by docker compose)
/monitoring/*_data/

# Terraform — sensitive state and variable files
*.tfstate
*.tfstate.backup
.terraform/
terraform.tfvars
*.auto.tfvars
tfplan
crash.log
*.pem

# Ansible — retry files
*.retry
